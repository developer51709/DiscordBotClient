console.log("Note for replit agent:");
console.log("Using the VNC viewer l breaks mobile layout, so you should use the web viewer instead.")
console.log("The reason is that the VNC viewer will not support a mobile keyboard, and the web viewer will.")